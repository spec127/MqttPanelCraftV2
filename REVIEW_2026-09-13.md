# 0.15.2 審查與手機驗證

審查範圍：`codex/usability-design-system` 自 `main@7fa0e27` 起尚未提交的修改，重點為 9/12 新增的 Mock 登入移除、Arduino 匯出、清理舊檔與畫布文字／明暗樣式；保留前次已安裝的相容性修正。

## 確認保留

- 移除未接後端的 Login/Register Activity、Layout 與 Manifest 註冊；Dashboard 仍是 Launcher，沒有殘留跳轉引用。
- 移除未註冊的 GaugeDefinition、無引用的 CalendarClockView 與非 source set 的 graphic.kt；Registry 仍為 22 個正式元件。
- 保留 0.15.1 的畫布英文文字與主題調整；Arduino 匯出仍為使用者選擇的 `.txt`，未擅自改成 ZIP。
- 時鐘觸發仍由 Service 負責；移除的是不再接線的 View local-trigger 入口。

## 本次修正

1. Web Arduino 匯出：直接字串 Topic（包含前導 `/`）不再被加上專案 prefix；可解析的常數及串接使用實際值，無法解析的動態表达式列警告，不猜路徑。
2. 產生器：C++ 字串與註解安全處理、控制字元、單次 placeholder 替換，避免使用者文字被當成另一層模板；固定 Locale.ROOT。
3. Broker／Topic 前綴／帳密固定陣列以 UTF-8 bytes 檢查容量；過長時回報明確錯誤，不產生截斷或不能編譯的陣列初始化式。
4. Setup：匯入後的 Arduino 匯出與儲存採相同 Topic 保留規則；未儲存提示納入元件、類型、程式與方向變動。
5. Arduino helper：損壞設定檔不再清空預設設定；舊檔缺少帳密 key 時保留 sketch 設定；空前綴不訂閱全站 `#`；去掉重連時重複訂閱 wildcard；MQTT buffer 設為 1024 bytes，超過訂閱上限會輸出明確提示。
6. 模板補正：搖桿 x/y JSON 與方向字串、色盤 HEX/RGB/HSV 解析、多序列 Chart 的設定 key JSON；相機的分塊說明改為 App 使用的 `CHUNK:1/3:data`（1-based、按順序傳送）。
7. Android 7：LED 減少狀態數量改用 `removeAt(lastIndex)`，避免 API 35 的 `List.removeLast()`。
8. About 崩潰紀錄只讀最近 64 KiB，避免隨日誌增長而無界讀取；修正匯出按鈕 tint 與側欄 tint 的 Lint 錯誤。

ArduinoJson 的數值檢查與錯誤處理依 [is<T>()](https://arduinojson.org/v7/api/jsonvariant/is/) 與 [deserializeJson()](https://arduinojson.org/v7/api/json/deserializejson/) 官方 API 核對。這不是 MCU 編譯或硬體測試的替代。

## 實際驗證

- 版本：`0.15.2`，versionCode `213`。
- 增量 `testDebugUnitTest assembleDebug assembleDebugAndroidTest lintDebug` 成功；沒有 clean、依賴重裝或全專案格式化。
- 單元測試：51 項，0 failures，0 errors。
- Lint：0 errors，909 warnings。倉庫原有 9/7 報告為 34 errors／1075 warnings；不是同一天對未修改基底重新跑出的比較。
- Android 7 真機：8 項測試通過，覆蓋 Launcher／移除 Mock 登入、17 個控制與感測元件預設匯出、Setup 表單與匯入 Topic、34 組切換列明暗圓角、ID 按鈕及背景卡片點擊、Web 狀態不重載、匯出匯入保留資料、22 元件預設 render。
- `adb install -r` 保留原 App data；安裝前備份位於倉庫外，安裝後 JSON 結構與值完整一致。備份與私人專案資料不納入 Git。
- 安裝後 Launcher 開啟 Dashboard 成功。
- APK SHA-256：`64037867391A213783AC76F773952ECEB3F365D400E5AABA7DC70C979E8AD486`。

## 仍有限制，不宣稱完成

- 未安裝 Arduino toolchain、未編譯 MCU 韌體、未燒錄開發板；生成內容的單元／手機測試不能證明所有 MCU 工具鏈相容。
- `.txt` 範例須配合 `arduino/newmanger/mqttpanel.h/.cpp` 與 PubSubClient、WiFiManager、ArduinoJson 7、對應 ESP board core；並非可單檔獨立編譯的完整套件。
- 相機擷取與 Base64 编碼仍為 ESP32 硬體接點；GPIO、實際感測值、馬達／LED 驅動依板型補上。Chart 中每個序列的讀值示例需換成對應感測來源。
- Web Topic 為有限靜態分析，不是完整 JavaScript 解譯器；函式參數、執行期字串等需人工補 handler。
- helper 儲存最多 32 個自訂訂閱；大型面板需調整 MQTTPANEL_MAX_SUBS。Topic 與 payload 合計需符合 MQTT buffer 容量。
- 尚未完成原大企劃中全部架構重構、視覺矩陣與所有硬體驗收；本次為可安裝審查修正版，不代表整個大企劃結案。
- 本次交付工作分支，不合併 main，不建立正式 GitHub Release。
