<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shift bottom down by -2dp to hide the bottom stroke -->
    <item android:bottom="-2dp">
         <!-- Wait, user wants NO BOTTOM border. Left/Right/Top needed. 
              If I shift bottom -2dp, the bottom line is drawn outside.
              But I also see user screenshot shows grid dots. 
              The border is 2dp. 
         -->
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/sheet_border_color" />
        </shape>
    </item>
</layer-list>
